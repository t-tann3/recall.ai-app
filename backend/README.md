# Backend

Node.js + Express skeleton. Deploy separately from the Vercel frontend (e.g. DigitalOcean).

```bash
npm install
cp .env.example .env
npm run dev
```

Add routes in `src/index.js` (or split into `src/routes/` when it grows).
